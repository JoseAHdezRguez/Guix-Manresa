---
// No props needed for this component
---

<section id="inicio" class="relative bg-gradient-to-br from-blue-900 via-blue-700 to-yellow-400 text-white pt-32 pb-24 min-h-screen flex items-center">
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0">
    <img 
      src="/assets/images/hero.jpg" 
      alt="Construcción GUIX Manresa" 
      class="w-full h-full object-cover mix-blend-overlay opacity-60"
    />
  </div>

  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-20">
    <svg width="100%" height="100%" viewBox="0 0 1200 600" class="w-full h-full">
      <rect fill="white" fill-opacity="0.05" width="1200" height="600"/>
      <g fill="white" fill-opacity="0.1">
        <circle cx="100" cy="100" r="50"/>
        <circle cx="800" cy="400" r="70"/>
        <circle cx="1100" cy="200" r="40"/>
      </g>
    </svg>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-6xl mx-auto px-4 text-center">
    <div class="max-w-4xl mx-auto">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 animate-fade-in-up">
        Construcción y Acabados de Calidad
      </h1>
      
      <p class="text-lg md:text-xl lg:text-2xl mb-8 opacity-90 animate-fade-in-up animation-delay-200">
        Más de 10 años de experiencia en Barcelona. Comprometidos con la excelencia en cada proyecto.
      </p>
      
      <a 
        href="/contacto" 
        class="inline-block bg-white text-blue-800 px-8 py-4 rounded-full font-bold text-lg hover:bg-gray-100 transform hover:-translate-y-1 transition-all duration-300 shadow-lg animate-fade-in-up animation-delay-400"
      >
        Solicitar Presupuesto
      </a>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 1s ease forwards;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
  }
</style>